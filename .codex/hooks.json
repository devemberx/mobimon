{
  "description": "Validate pull-request merge commands.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$(git rev-parse --show-toplevel)/scripts/validate_merge.py\"",
            "timeout": 30,
            "statusMessage": "Checking pull-request merge policy"
          }
        ]
      }
    ]
  }
}
