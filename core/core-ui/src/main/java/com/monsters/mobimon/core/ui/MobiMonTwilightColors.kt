package com.monsters.mobimon.core.ui

import androidx.compose.material3.darkColorScheme

/** Semantic roles used by the app, preview and Debug theme. */
val MobiMonTwilightColors =
    darkColorScheme(
        primary = MobiMonColors.button,
        onPrimary = MobiMonColors.onButton,
        primaryContainer = MobiMonColors.raised,
        onPrimaryContainer = MobiMonColors.text,
        secondary = MobiMonColors.accent,
        onSecondary = MobiMonColors.onButton,
        secondaryContainer = MobiMonColors.raised,
        onSecondaryContainer = MobiMonColors.text,
        tertiary = MobiMonColors.success,
        onTertiary = MobiMonColors.onButton,
        tertiaryContainer = MobiMonColors.raised,
        onTertiaryContainer = MobiMonColors.success,
        background = MobiMonColors.background,
        onBackground = MobiMonColors.text,
        surface = MobiMonColors.panel,
        onSurface = MobiMonColors.text,
        surfaceVariant = MobiMonColors.raised,
        onSurfaceVariant = MobiMonColors.muted,
        outline = MobiMonColors.border,
        outlineVariant = MobiMonColors.border,
        error = MobiMonColors.destructive,
        onError = MobiMonColors.onButton,
        errorContainer = MobiMonColors.raised,
        onErrorContainer = MobiMonColors.destructive,
        inverseSurface = MobiMonColors.button,
        inverseOnSurface = MobiMonColors.onButton,
        inversePrimary = MobiMonColors.onButton,
        surfaceTint = MobiMonColors.accent,
    )
